netty 框架中，在协议传输这块，除了可以使用“特殊字符串”，以及“定长报文”，的解决方案之外，还提供了一致解决方案，就是使用序列化的对象来完成数据的传输


当然，它序列化的方案，不是简单的采用JDK的序列化手段，而是使用jboss的marshalling序列化框架来实现，带来的好处：就是我们在传输时，不再是以字符串为主，或者是bytebuf为主，而是以JAVA对象来完成数据传输，我每传输一次，都是一个新的JAVA对象， 这样就避免了“数据报文的粘包”


